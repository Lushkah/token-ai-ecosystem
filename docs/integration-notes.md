# v0.4 Integration Notes

- The supplied `install-deps.sh` is integrated at `mining/intel-arc/install-deps.sh`.
- A mobile Mining Control screen is available at `/mining`.
- The mobile app does not execute GPU mining; it is a monitoring/control UI.
- The supplied FUNDING.yml is intentionally not copied into Lushka because its
  sponsorship identifiers belong to another project/person.
